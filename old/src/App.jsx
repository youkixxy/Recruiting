// App.jsx — entry point
// The full Resume Suite is self-contained in MasterLibrary.jsx
// (already has its own 3-tab navigation: 母版库 / 分析 / Tailor)
import MasterLibrary from './MasterLibrary.jsx'
export default function App() {
  return <MasterLibrary />
}
